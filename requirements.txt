# FastAPI和相关依赖
fastapi==0.68.0
uvicorn[standard]==0.15.0
python-multipart==0.0.5

# 数据验证 - 使用v1版本避免Rust编译问题
pydantic==1.10.12

# 支付宝SDK（可选，用于更完整的支付功能）
alipay-sdk-python==3.7.779

# 加密相关 - 使用较旧但稳定的版本避免Rust编译问题
cryptography==3.4.8

# 其他工具
requests==2.31.0
python-dateutil==2.8.2