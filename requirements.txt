# FastAPI和相关依赖
fastapi==0.65.2
uvicorn==0.13.4
python-multipart==0.0.5

# 数据验证 - 使用更早期版本确保Python 3.13兼容性
pydantic==1.8.2

# 支付宝SDK
alipay-sdk-python==3.7.779

# 加密相关 - 使用较旧但稳定的版本避免Rust编译问题
cryptography==3.4.8

# 其他工具
requests==2.31.0
python-dateutil==2.8.2